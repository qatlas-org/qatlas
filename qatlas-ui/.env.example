# Leave empty if the app is served behind the same nginx/reverse proxy as the
# backend (recommended for prod - same origin, no CORS). Otherwise set to the
# backend's base URL, e.g. http://backend:8080
VITE_API_BASE_URL=
